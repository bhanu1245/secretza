/**
 * City SEO V6 Profile — re-export from profile registry.
 *
 * This file exists so seo-profiles/ can hold per-profile definitions
 * as the profile library grows. New profiles are added here as new files;
 * CITY_SEO_V6_PROFILE is defined in seo-profile-registry.ts alongside
 * the registry so the scaffold compiles without a circular dep.
 */

export { CITY_SEO_V6_PROFILE } from "@/lib/seo-profile-registry";
