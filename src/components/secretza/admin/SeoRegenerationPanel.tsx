'use client';

import SeoStudio from '@/components/secretza/admin/seo-studio/SeoStudio';

/** SEO Regeneration — now powered by SEO Studio (backward-compatible entry point). */
export default function SeoRegenerationPanel() {
  return <SeoStudio />;
}
